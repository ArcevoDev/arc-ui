# npm auth is handled by changesets/action in CI (GitHub secret NPM_TOKEN)
# For local publishing, run: pnpm config set "//registry.npmjs.org/:_authToken" <token>

# Storybook requires framework packages resolvable from its CJS entry point
public-hoist-pattern[]=*storybook*
